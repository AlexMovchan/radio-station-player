import React from 'react';
import { StyledRotateContainer } from './style';
const RotateAnimation = ({ favoriteList, activeStation, setActiveRadiostation }) => {
    return (
        <StyledRotateContainer />
    )
  }
  
  export default RotateAnimation;
  