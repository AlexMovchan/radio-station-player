import styled from 'styled-components';

export const Header = styled.header `
	display: flex;
    flex-flow: row wrap;
    justify-content: center;
    background-image: linear-gradient(darkgrey, #383838);
`;